{
  "buildCommand": "pnpm --filter @workspace/yusstyle-portfolio run build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "artifacts/yusstyle-portfolio/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}